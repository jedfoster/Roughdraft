<section class="content instapaper-body">
  <header>
    <h1>Roughdraft. Gist-powered writing.</h1>
  </header>

  <p>Sometimes you have something to say that's longer than a Tweet, but too small for a blog. Enter Roughdraft. The concept is simple: writing for the web, backed by <a href="https://gist.github.com">GitHub Gists</a>.</p>

  <h2 id="how_it_works">How it works</h2>

  <ol>
    <li>Create a public, <a href="http://daringfireball.net/projects/markdown/">Markdown</a>-formatted <a href="https://gist.github.com">Gist on Github</a> with at least one file.</li>
    <li>Note the Gist ID. It’s at the end of the URL: <code class="no-prettify">gist.github.com/jedfoster/<b>4339935</b></code>.</li>
    <li>See your Gist, made pretty, at <code>roughdraft.io/<b>gist-id</b></code></li>
  </ol>

  <h5 id="bookmarklet_steps"><b>Or</b></h5>

  <ol>
    <li>Drag the bookmarklet to your bookmarks bar.</li>
    <li>From a Gist page, click the bookmarklet to open the Gist in Roughdraft.
    <li>Click it elsewhere on the web to append Roughdraft links to every Gist link on the page.</li>
  </ol>

  <p id="roughdraft-bookmarklet"><a href="javascript: (function(e,a,g,h,f,c,b,d){if(!(f=e.jQuery)||g>f.fn.jquery||h(f)){c=a.createElement('script');c.type='text/javascript';c.src='http://ajax.googleapis.com/ajax/libs/jquery/'+g+'/jquery.min.js';c.onload=c.onreadystatechange=function(){if(!b&&(!(d=this.readyState)||d=='loaded'||d=='complete')){h((f=e.jQuery).noConflict(1),b=1);f(c).remove()}};a.documentElement.childNodes[0].appendChild(c)}})(window,document,'1.4.2',function($,L){var gist_re=/^https:\/\/gist\.github\.com\/([\w-]+)*?\/*?(\d+)$/i,rel_re=/^\/?(\d+)$/,on_gist=gist_re.test(location.href);if(on_gist){i=location.pathname.split(/\/([\w-]+)\/(\d+)/);location.href='http://'+i[1]+'.roughdraft.io/'+i[2];}else{$('a').each(function(){var b=$(this).attr('href')||'',a=b.match(gist_re);if(on_gist&&!(a&&a[2])){a=b.match(rel_re)} if(a&&a[2]){if(typeof(a[1])=='undefined'){a[1]='www';} $(this).after(' <a href=&quot;http://'+a[1]+'.roughdraft.io/'+a[2]+'&quot;>[roughdraft.io]</a>')}});}});" title="Drag me to your bookmarks bar.">Roughdraft Bookmarklet</a></p>

  <h3 id="formatting">Formatting</h3>

  <ul>
    <li>Roughdraft uses <a href="https://help.github.com/articles/github-flavored-markdown" target="new">GitHub-flavored Markdown</a>. In addition to all of the usual Markdown syntax, GFM supports fenced code blocks and syntax highlighting.</li>
    <li>Use images and even <a href="http://www.emoji-cheat-sheet.com/" target="new">emoji</a>! <img class="emoji" title=":metal:" alt=":metal:" src="/images/emoji/metal.png" height="20" width="20" align="absmiddle"></li>
    <li>Your Gist&#8217;s <code>description</code> field will be used as the headline for your post.</li>
  </ul>

  <p>More detailed information on formatting our Gist is coming soon.</p>

  <h3 id="user_pages">User Pages</h3>

  <p>Roughdraft is built with the GitHub API, so we can do some really cool stuff, like give every GitHub user their own Roughdraft namespace!</p>

  <p>Visit <code><b>username</b>.roughdraft.io</code> to see a list of all the public, Markdown-formatted Gists belonging to <code><b>username</b></code>.</p>

  <h2 id="about_roughdraft">About Roughdraft</h2>

  <p>Roughdraft is project of <a href="http://jedfoster.com/">Jed Foster</a>. The <a href="https://github.com/jedfoster/Roughdraft/">source code for Roughdraft</a> is available on GitHub. </p>

  <p class="legal">All content on Roughdraft is copyrighted by, and belongs to, the respective author(s). Roughdraft is not affiliated with GitHub.</p>

  <h3 id="mad_props">Mad Props</h3>

  <p>This project was inspired by, and borrows code from, <a href="http://gazit.me">Idan Gazit&#8217;s</a> <a href="http://gist.io">gist.io</a>. My thanks to Idan for the original. </p>

</section>